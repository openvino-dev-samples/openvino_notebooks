# Local-only artifacts (HuggingFace snapshot + converted IR + run outputs).
# Keep these out of version control - they're regenerated by the
# convert_pipeline + nbconvert steps in the notebook.
Wan-Dancer-14B-snapshot/
model/
executed.ipynb
output_global.mp4
output_final.mp4
__pycache__/
